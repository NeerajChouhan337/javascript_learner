const name = "Neeraj";
const repoCount = 20;
// console.log(name +  repoCount + "Value");
console.log(`${name} ${repoCount} value`);

// const Name = new String("neeraj"); 

// Strings Method
// subString
// slice
// indexOf
// replace
// includes 
// trim
